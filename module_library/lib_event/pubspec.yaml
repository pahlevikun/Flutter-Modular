name: lib_event
description: Event & tracking module.
version: 1.0.0
author: pahlevikun
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core_utilities:
    path: ../../module_core/core_utilities
  foundation_injector:
    path: ../../module_foundation/foundation_injector

dev_dependencies:
  build_runner:
  injectable_generator:

flutter: